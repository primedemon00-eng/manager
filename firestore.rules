rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow reading and writing to all collections for the bot and dashboard
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
